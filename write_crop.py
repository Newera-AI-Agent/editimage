content = '''\
'use client';

import { useState, useCallback, useRef } from 'react';
import { CropRect } from '@/lib/types';

interface Props {
  imageWidth: number;
  imageHeight: number;
  zoom: numbe…(4180 chars)